- run: python tracker.py

      - name: Save product state
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add products.json
          git commit -m "update products" || echo "no change"
          git push
